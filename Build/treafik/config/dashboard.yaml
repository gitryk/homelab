http:
  routers:
    dashboard:
      entryPoints: [websecure]
      service: api@internal
      middlewares: [mw-stack]
      rule: 'Host(`devops.{{ env "DOMAIN" }}`)'
      tls:
        certResolver: step
