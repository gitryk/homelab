http:
  routers:
    dashboard:
      entryPoints: [websecure]
      service: api@internal
      middlewares: [mw-stack]
      rule: 'Host(`in-service.{{ env "DOMAIN" }}`)'
      tls:
        certResolver: step
